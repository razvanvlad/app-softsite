# Architecture Diagram - Softsite AI

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    React Frontend                         │  │
│  │                    (Vite + React 19)                      │  │
│  │                                                            │  │
│  │  Components:                                              │  │
│  │  • Dashboard                                              │  │
│  │  • ConsultationBot ──────┐                               │  │
│  │  • StartupEligibility ───┼─── useAuth() ───┐            │  │
│  │  • SeoAnalyzer ──────────┤                 │            │  │
│  │  • BusinessTools ────────┤                 │            │  │
│  │  • Settings ─────────────┘                 │            │  │
│  │                                             │            │  │
│  │  ┌────────────────────────────────────┐   │            │  │
│  │  │      AuthContext                   │◄──┘            │  │
│  │  │  (Supabase Auth Client)            │                │  │
│  │  │  - user state                      │                │  │
│  │  │  - signIn/signOut                  │                │  │
│  │  └────────────────────────────────────┘                │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────┐                │  │
│  │  │   geminiService.ts                 │                │  │
│  │  │  - streamChatResponse()            │                │  │
│  │  │  - analyzeSeoWithGemini()          │                │  │
│  │  │  - analyzeSpeedWithGemini()        │                │  │
│  │  │  - generateBudgetPlan()            │                │  │
│  │  └────────────────────────────────────┘                │  │
│  │                      │                                     │  │
│  │                      │ fetch('/api/...')                  │  │
│  └──────────────────────┼─────────────────────────────────┘  │
│                         │                                      │
└─────────────────────────┼──────────────────────────────────────┘
                          │
                          │ HTTPS
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    VERCEL PLATFORM                               │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Serverless Functions (/api)                  │  │
│  │                                                            │  │
│  │  ┌─────────────────────────────────────────────────┐    │  │
│  │  │  api/chat.ts                                     │    │  │
│  │  │  ┌─────────────────────────────────────────┐   │    │  │
│  │  │  │ 1. Receive: message, history, userId    │   │    │  │
│  │  │  │ 2. Call Gemini API with context         │───┼────┼──┐
│  │  │  │ 3. Save to Supabase chat_sessions       │───┼────┼──┼─┐
│  │  │  │ 4. Return AI response                   │   │    │  │ │
│  │  │  └─────────────────────────────────────────┘   │    │  │ │
│  │  └─────────────────────────────────────────────────┘    │  │ │
│  │                                                            │  │ │
│  │  ┌─────────────────────────────────────────────────┐    │  │ │
│  │  │  api/analyze.ts                                  │    │  │ │
│  │  │  ┌─────────────────────────────────────────┐   │    │  │ │
│  │  │  │ 1. Receive: type, data                  │   │    │  │ │
│  │  │  │ 2. Route to correct analyzer:           │   │    │  │ │
│  │  │  │    - SEO (url, keyword)                 │   │    │  │ │
│  │  │  │    - Speed (url)                        │───┼────┼──┤ │
│  │  │  │    - Budget (industry, location)        │   │    │  │ │
│  │  │  │ 3. Call Gemini with schema validation   │   │    │  │ │
│  │  │  │ 4. Return structured JSON               │   │    │  │ │
│  │  │  └─────────────────────────────────────────┘   │    │  │ │
│  │  └─────────────────────────────────────────────────┘    │  │ │
│  │                                                            │  │ │
│  │  ┌─────────────────────────────────────────────────┐    │  │ │
│  │  │  api/stripe-webhook.ts                           │    │  │ │
│  │  │  (Placeholder for future payment integration)    │    │  │ │
│  │  └─────────────────────────────────────────────────┘    │  │ │
│  │                                                            │  │ │
│  │  Environment Variables (Secrets):                        │  │ │
│  │  • GEMINI_API_KEY                                        │  │ │
│  │  • SUPABASE_SERVICE_ROLE_KEY                             │  │ │
│  │  • STRIPE_SECRET_KEY                                     │  │ │
│  └──────────────────────────────────────────────────────────┘  │ │
│                                                                  │ │
└──────────────────────────────────────────────────────────────────┘ │
                          │                                          │ │
                          │                                          │ │
                          ▼                                          │ │
┌─────────────────────────────────────────────────────────────────┐ │ │
│                   GOOGLE GEMINI API                              │ │ │
│                                                                  │ │ │
│  ┌──────────────────────────────────────────────────────────┐  │ │ │
│  │  Model: gemini-2.5-flash                                  │  │ │ │
│  │  • Chat with context (Start-up Nation expert)            │◄─┘ │
│  │  • Structured output (SEO, Speed, Budget)                │    │
│  │  • Temperature: 0.7                                       │    │
│  └──────────────────────────────────────────────────────────┘  │  │
│                                                                  │  │
└──────────────────────────────────────────────────────────────────┘  │
                                                                      │
                          ┌───────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SUPABASE                                    │
│                  (PostgreSQL + Auth)                             │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  Authentication                           │  │
│  │  • Email/Password                                         │  │
│  │  • Google OAuth (configurable)                            │  │
│  │  • JWT tokens                                             │  │
│  │  • Session management                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  Database Tables                          │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │  profiles                                           │  │  │
│  │  │  ─────────────────────────────────────────────────  │  │  │
│  │  │  id (UUID, FK to auth.users)                       │  │  │
│  │  │  email, full_name, company_name                    │  │  │
│  │  │  cui, industry, plan                               │  │  │
│  │  │  created_at, updated_at                            │  │  │
│  │  │                                                     │  │  │
│  │  │  RLS: Users can only see their own profile         │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │  chat_sessions                                      │  │  │
│  │  │  ─────────────────────────────────────────────────  │  │  │
│  │  │  id (UUID)                                         │  │  │
│  │  │  user_id (FK to auth.users)                        │  │  │
│  │  │  role (user/assistant/system)                      │  │  │
│  │  │  content (TEXT)                                    │  │  │
│  │  │  created_at                                        │  │  │
│  │  │                                                     │  │  │
│  │  │  RLS: Users can only see their own chats           │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │  eligibility_checks                                 │  │  │
│  │  │  ─────────────────────────────────────────────────  │  │  │
│  │  │  id (UUID)                                         │  │  │
│  │  │  user_id (FK to auth.users)                        │  │  │
│  │  │  is_eligible (BOOLEAN)                             │  │  │
│  │  │  score (INTEGER 0-100)                             │  │  │
│  │  │  details (JSONB)                                   │  │  │
│  │  │  created_at                                        │  │  │
│  │  │                                                     │  │  │
│  │  │  RLS: Users can only see their own checks          │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Examples

### Example 1: User Sends Chat Message

```
1. User types message in ConsultationBot
   ↓
2. Frontend calls geminiService.streamChatResponse(history, message, userId)
   ↓
3. geminiService makes POST to /api/chat
   ↓
4. Vercel Function receives request
   ↓
5. Function calls Gemini API with context
   ↓
6. Gemini returns AI response
   ↓
7. Function saves to Supabase chat_sessions (2 rows: user + assistant)
   ↓
8. Function returns response to frontend
   ↓
9. Frontend displays AI message
```

### Example 2: User Completes Eligibility Check

```
1. User fills out 3-step form in StartupEligibility
   ↓
2. User clicks "Get Smart Budget Plan" (Step 2 → 3)
   ↓
3. Component calls saveResults() function
   ↓
4. Function calculates score from form data
   ↓
5. Supabase.insert() to eligibility_checks table
   ↓
6. Data saved with user_id, score, and details (JSON)
   ↓
7. User proceeds to Step 3 (Budget Generator)
```

### Example 3: SEO Analysis

```
1. User enters URL and keyword in SeoAnalyzer
   ↓
2. Frontend calls analyzeSeoWithGemini(url, keyword)
   ↓
3. geminiService makes POST to /api/analyze with type='seo'
   ↓
4. Vercel Function routes to SEO analyzer
   ↓
5. Function calls Gemini with schema validation
   ↓
6. Gemini returns structured JSON:
   { url, score, title, recommendations[], keywords[] }
   ↓
7. Function returns JSON to frontend
   ↓
8. Frontend displays score, chart, and recommendations
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 1: Frontend (Public)                                 │
│  • Only VITE_* environment variables exposed                │
│  • Supabase anon key (limited permissions)                  │
│  • No direct API access to Gemini or Stripe                 │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 2: Vercel Functions (Private)                        │
│  • GEMINI_API_KEY (never exposed to client)                 │
│  • SUPABASE_SERVICE_ROLE_KEY (full database access)         │
│  • STRIPE_SECRET_KEY (payment processing)                   │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  Layer 3: Supabase RLS (Row Level Security)                 │
│  • Users can only access their own data                     │
│  • Policies enforce user_id = auth.uid()                    │
│  • Service role bypasses RLS (backend only)                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Technology Stack

```
Frontend:
├── React 19
├── TypeScript
├── Vite (build tool)
├── React Router (navigation)
├── Tailwind CSS (styling)
├── Recharts (data visualization)
└── Lucide React (icons)

Backend:
├── Vercel Serverless Functions
├── Node.js runtime
└── TypeScript

Database & Auth:
├── Supabase (PostgreSQL)
├── Row Level Security
└── JWT authentication

AI:
├── Google Gemini 2.5 Flash
└── Structured output with schemas

Payments (Future):
└── Stripe
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      VERCEL CDN                              │
│  • Global edge network                                      │
│  • Automatic HTTPS                                          │
│  • Static assets cached                                     │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                   VERCEL FUNCTIONS                           │
│  • Auto-scaling                                             │
│  • Cold start < 1s                                          │
│  • Region: Auto (closest to user)                           │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    SUPABASE                                  │
│  • Region: Europe West (for Romania)                        │
│  • Connection pooling                                       │
│  • Automatic backups                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 📈 Scalability

| Component | Free Tier | Scaling Strategy |
|-----------|-----------|------------------|
| Vercel Functions | 100GB-hrs/mo | Auto-scales, pay per use |
| Supabase DB | 500MB | Upgrade to Pro ($25/mo) |
| Gemini API | Free quota | Pay per token after quota |
| Bandwidth | 100GB/mo | CDN caching reduces usage |

---

## 🎯 Performance Targets

| Metric | Target | Actual (Expected) |
|--------|--------|-------------------|
| Page Load | < 2s | ~1.5s |
| API Response | < 5s | 2-4s (Gemini dependent) |
| Database Query | < 100ms | ~50ms |
| Authentication | < 1s | ~500ms |
| Time to Interactive | < 3s | ~2s |

---

This architecture provides:
✅ **Security**: API keys protected, RLS enforced  
✅ **Scalability**: Auto-scaling serverless functions  
✅ **Performance**: Edge CDN, fast database  
✅ **Reliability**: Managed services, automatic backups  
✅ **Cost-Effective**: Generous free tiers, pay-as-you-grow
